'use client';

/**
 * Admin users section placeholder. Delivers the client shell for the user
 * management table. S2 wires the data, search, role picker, and ban toggle.
 */

export default function UsersSection() {
  return (
    <section data-testid="admin-users-section">
      <h2 className="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-4">Users</h2>
      <p className="text-sm text-gray-500 dark:text-gray-400">User management coming in S2.</p>
    </section>
  );
}
